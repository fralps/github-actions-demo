name: hello-world
on: push

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Bash
        run: echo "Hello World!"
      - name: Ruby
        uses: actions/checkout@v2
        uses: actions/setup-ruby@v1
        with:
          ruby-version: '2.6'
        run: ruby hello.rb
